# نمونهٔ متغیرهای محیط برای اجرای Docker — کپی به .env در ریشهٔ پروژه یا تنظیم در سیستم
# برای docker-compose از پوشهٔ team13 یا ریشه استفاده کنید.

# پورتی که gateway (nginx) روی آن گوش می‌دهد؛ پیش‌فرض 8080
TEAM_PORT=8080

# کلیدهای نشان — برای مسیریابی، نقشه، جستجو و Geocoding (استفاده در Docker)
# اگر این مقادیر را در .env بگذارید یا به compose پاس دهید، سرویس با نقشه و API نشان ران می‌شود.
NESHAN_API_KEY_SERVICE=service.4b8184b63cdb408b90a3a31feaabc6a8
NESHAN_API_KEY_WEB=web.5ee1e82db9314ba5a50806d13018c9a1

# اختیاری: کلید مخفی Django
# DJANGO_SECRET_KEY=your-secret-key

# اختیاری: اگر سرویس Core جداگانه دارید
# CORE_BASE_URL=http://core:8000

# شبکهٔ app404 باید از قبل وجود داشته باشد. از ریشهٔ پروژه:
# docker network create app404_net
